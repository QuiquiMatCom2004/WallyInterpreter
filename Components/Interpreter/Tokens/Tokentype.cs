namespace WallyInterpreter.Components.Interpreter.Tokens
{
    public enum Tokentype
    {
        Garbage,
        Keyword,
        Symbol,
        Operator,
        Identifier,
        Number,
        Boolean,
        String,
        EOL
    }
}
